const sortedNewArray=require('./10.2')

describe("test if Ivan has the max score",()=>{
    test('if there is a student name Ivan',()=> {
        
    expect(sortedNewArray).toContain('Ivan');
    // });
    // test ('if there is student name Ivan?', () => {
    expect(new Set(sortedNewArray)).toContain('Ivan');
    }
    )}
)
    describe ("Ivan has the max score",()=>{
    test('if student Ivan has score 35',()=> {
        expect(new Set(sortedNewArray)).toContain('35')
    }
    )}
    )